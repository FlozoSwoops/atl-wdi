import React, {Component} from 'react';

import splash from '../images/splashpizza.jpg'

class Splash extends Component {
    render() {
        return(
            <img src={ splash } />
        )
    }
}

export default Splash;